project 'SpeakTimesTable.xcodeproj'

# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'SpeakTimesTable' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for SpeakTimesTable
  
  pod 'IQKeyboardManager', '~> 5.0.7', :inhibit_warnings => true
  pod 'CMPopTipView', '~> 2.3.2', :inhibit_warnings => true
  pod 'UIColor-HexUtils', '~> 1.1', :inhibit_warnings => true
  pod 'SWFrameButton', '~> 1.2.2', :inhibit_warnings => true
end
